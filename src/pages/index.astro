---
import BaseLayout from '../layouts/BaseLayout.astro'
import BlogPost from '../components/BlogPost'
import World from '../components/World'
import GitHub from '../components/Github'
import SphereComponent from '../components/SphereComponent'
import MarqueeWithImages from '../components/Marquee'
const allPosts = await Astro.glob('../pages/posts/*.md')
const pageTitle =
  'Pooya Golchian - Senior Web Developer Specializing in React.js, Vue.js & DevOps | Portfolio'
---

<BaseLayout pageTitle={pageTitle}>
  <div class="hidden lg:flex">
    <World client:only="react" />
  </div>

  <div class="flex flex-col lg:flex-row lg:hidden">
    <div
      class="md:w-11/12 m-auto container px-5 py-5 mx-auto w-full relative mb-5"
    >
      <h1 class="text-2xl font-bold mb-4 h1">Design, build, ship</h1>
      <p class="mb-4">
        If you're starting a new project or improving an existing product suite,
        I bring the knowledge and practical skills needed to advance your
        business goals. I specialize in React, Next.js, Astro, and Nest.js. My
        work in design systems, content management systems, and front-end
        infrastructure has received international recognition. I focus on
        enhancing team productivity and following industry best practices. Reach
        out to me—I'm enthusiastic about collaborating with you.
      </p>
    </div>
  </div>

  <div
    class="md:w-11/12 m-auto container px-5 py-5 mx-auto w-full relative mb-5"
  >
    <h3 class="h2 py-10">Blog</h3>
    <ul
      class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-10 blog-posts"
    >
      {
        allPosts.map((post) => {
          return (
            <BlogPost
              cover={post.frontmatter.cover}
              coverAlt={post.frontmatter.coverAlt}
              description={post.frontmatter.description}
              url={post.url}
              title={post.frontmatter.title}
            />
          )
        })
      }
    </ul>
  </div>

  <div
    class="md:w-11/12 m-auto container px-5 py-5 mx-auto w-full relative mb-5"
  >
    <div class="flex justify-start align-middle flex-col">
      <h3 class="h2">Github</h3>
      <div class="md:w-11/12 m-auto container w-full relative mb-5 mx-auto">
        <GitHub client:only="react" />
      </div>
    </div>
  </div>

  <div class="hidden lg:block">
    <SphereComponent client:only="react" />
  </div>
  <MarqueeWithImages client:only="react" />
</BaseLayout>
